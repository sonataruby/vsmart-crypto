
<div class="row row-cols-1 row-cols-md-3 g-4">
<% items.forEach(function(data, index){ %>
      <div class="col">
      <div class="card">

       <a href="/market/token/<%=data.nft%>/<%=data.tokenId%>">
       <div class="card-zoom">
        <img src="<%= data.image%>" alt="..." class="zoom card-img-top">
       </div>
         <div class="card-body">
           
           <h5 class="card-title">#<%= data.tokenId%> | <%= data.name%> </h5>
              
              <div class="row">
                    <div class="col-6">Lever: <%=data.attributes.Lever%></div>
                    <div class="col-6">Power: <%=data.attributes.Power%></div>
                    <div class="col-12">Acceleraction: <%=data.attributes.Acceleraction%></div>
                    <div class="col-6">Handing: <%=data.attributes.Handing%></div>
                    <div class="col-6">Nitro: <%=data.attributes.Nitro%></div>
              </div>
               <p class="card-text"><small class="text-muted"><%=data.startTime%></small></p>

           <div class="d-flex justify-content-between">
              <b><%= data.price%> <%= token.name%></b> <a href="/market/token/<%=data.nft%>/<%=data.tokenId%>"  class="btn btn-sm btn-primary">BUY</a>
              </div>
         </div>
         </a>
       </div>
      </div>
<% }); %>
</div>

<style>
.card-hoder{
    padding:0px !important;
}
.card-zoom{
    overflow: hidden;
}
.zoom {
  -webkit-transition: all 0.3s ease-in-out;
        -moz-transition:    all 0.3s ease-in-out;
        -o-transition:      all 0.3s ease-in-out;
        -ms-transition:     all 0.3s ease-in-out;
        transition:         all 0.3s ease-in-out;
  transition: all .2s ease-in-out; 
  margin: 0 auto;
}

.card-hoder:hover .zoom {
  transform: scale(1.5); /* (150% zoom - Note: if the zoom is too large, it will go outside of the viewport) */
}
</style>